pipeline {
agent any
 
options {
    skipDefaultCheckout()
}

stages {
stage ('Checkout') {
        steps{
            checkout(scm)
            echo "checkout"
        }
    }
stage('test'){
        steps{
            checkout(scm)
            echo "Branch name  is: ${env.BRANCH_NAME}"
        }
    }
stage ('Restore Packages') {     
         steps {
             echo "restore"            
          }
        }

stage('Build') {
     steps {
            echo "build"
      }
   }
stage('Deploy') { 
 	steps {
 		echo "deploy"
 		}  
    }
 }
// post{
//    always{}
//    success{}
//  failure{}
// }
}

